
extends template/layout.pug 

block content 
    .container.justify-content-between 
        div(id="fetch_container", onload="loadMovies()")

block scripts 
    script.
        function loadMovies()
        {
            var container = document.getElementById("fetch-container");
            container.innerHTML += "Hola";
            alert(1);
            console.log("Funcion cargada");
        }
